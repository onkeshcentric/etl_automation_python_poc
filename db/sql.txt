CREATE TABLE claims(
ID INTEGER PRIMARY KEY,
CLAIM_ID TEXT NOT NULL,
MEMBER_ID TEXT NOT NULL,
START_DATE DATE NOT NULL,
END_DATE DATE NOT NULL,
CLAIM_AMOUNT REAL NOT NULL);

INSERT INTO claims(ID, CLAIM_ID, MEMBER_ID, START_DATE, END_DATE, CLAIM_AMOUNT) values(?,?,?,?,?,?);

CREATE TABLE members(
ID INTEGER PRIMARY KEY,
FIRST_NAME TEXT NOT NULL,
LAST_NAME TEXT NOT NULL,
DOB DATE NOT NULL,
MEMBER_ID TEXT NOT NULL,
SSN INTEGER NOT NULL,
ADDRESS1 TEXT NOT NULL,
ADDRESS2 TEXT NOT NULL,
CITY TEXT NOT NULL,
STATE TEXT NOT NULL,
ZIP TEXT NOT NULL);

INSERT INTO members(ID, FIRST_NAME, LAST_NAME, DOB, MEMBER_ID, SSN, ADDRESS1, ADDRESS2, CITY, STATE, ZIP) values(?,?,?,?,?,?,?,?,?,?,?);

CREATE TABLE reports(
ID INTEGER PRIMARY KEY,
NAME TEXT NOT NULL,
DOB DATE NOT NULL,
MEMBER_ID TEXT NOT NULL,
SSN INTEGER NOT NULL,
ADDRESS TEXT NOT NULL,
CITY TEXT NOT NULL,
STATE TEXT NOT NULL,
ZIP TEXT NOT NULL,
CLAIM_COUNT INTEGER NOT NULL,
TOTAL_AMOUNT REAL NOT NULL);

INSERT INTO reports(ID, NAME, DOB, MEMBER_ID, SSN, ADDRESS, CITY, STATE, ZIP, CLAIM_COUNT, TOTAL_AMOUNT) values(?,?,?,?,?,?,?,?,?,?,?);
